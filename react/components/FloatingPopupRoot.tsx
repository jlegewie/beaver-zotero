import React from 'react';
import FloatingPopupContainer from './ui/popup/FloatingPopupContainer';

const FloatingPopupRoot: React.FC = () => {
    return <FloatingPopupContainer />;
};

export default FloatingPopupRoot;
